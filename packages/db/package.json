{
  "name": "@syncpad/db",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "pnpm exec prisma generate && (mkdir -p ../../apps/web/.prisma && cp -r ../../node_modules/.pnpm/@prisma+client*/node_modules/.prisma/client ../../apps/web/.prisma/ || true) && tsc",
    "typecheck": "tsc --noEmit",
    "generate": "pnpm exec prisma generate",
    "migrate": "prisma migrate dev",
    "lint": "echo 'No lint script for db package yet'"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "prisma": "^5.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5"
  }
}
